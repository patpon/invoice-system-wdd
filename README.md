# ระบบพิมพ์บิลและใบกำกับภาษี (Bill & Tax Invoice System)

โปรแกรมเว็บแอปพลิเคชันสำหรับพิมพ์บิลและใบกำกับภาษี พร้อมเชื่อมต่อ Google Sheets

## 🚀 เริ่มต้นใช้งาน

### วิธีที่ 1: เปิดไฟล์โดยตรง
1. เปิดไฟล์ `index.html` ด้วย Browser (Chrome, Firefox, Edge)
2. ระบบพร้อมใช้งานทันที

### วิธีที่ 2: ใช้ Live Server (แนะนำ)
หากใช้ VS Code สามารถติดตั้ง extension "Live Server" และ right-click ที่ `index.html` > "Open with Live Server"

---

## ⚙️ ตั้งค่าการเชื่อมต่อ Google Sheets

### ขั้นตอนที่ 1: สร้าง Google Sheet
1. สร้าง Google Sheet ใหม่
2. ตั้งชื่อ Sheet แรกเป็น "Customers"
3. เพิ่ม Header ในแถวแรก:
   | รหัสลูกค้า | ชื่อลูกค้า | ที่อยู่ | เลขผู้เสียภาษี | เบอร์โทร | Email |

4. ตั้งค่าการแชร์เป็น **"Anyone with the link can view"**

### ขั้นตอนที่ 2: ตั้งค่า Google Apps Script (สำหรับบันทึกข้อมูล)
1. ที่ Google Sheet ไปที่ **Extensions > Apps Script**
2. ลบโค้ดเดิมในหน้า Code.gs
3. Copy โค้ดจากไฟล์ `google-apps-script.js` ไปวาง
4. กด **Save** (Ctrl+S)
5. กด **Deploy > New deployment**
6. เลือก Type: **Web app**
7. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
8. กด **Deploy**
9. Copy URL ที่ได้ไปใส่ในการตั้งค่าระบบ

### ขั้นตอนที่ 3: ตั้งค่าในระบบ
1. เปิดโปรแกรมและกดปุ่ม ⚙️ (ตั้งค่า)
2. ไปที่แท็บ "Google Sheets"
3. ใส่ URL ของ Google Sheet
4. ใส่ URL ของ Apps Script Web App
5. กด "ทดสอบการเชื่อมต่อ"
6. กด "บันทึกการตั้งค่า"

---

## 📖 คุณสมบัติ

| ฟีเจอร์ | รายละเอียด |
|--------|-----------|
| 🔗 Google Sheets | เชื่อมต่อฐานข้อมูลลูกค้า |
| 🖼️ โลโก้ร้าน | อัปโหลดและเปลี่ยนได้ |
| 👥 จัดการลูกค้า | เพิ่ม/แก้ไข พร้อม sync กับ Google Sheets |
| 🔍 ประวัติใบกำกับ | ค้นหาย้อนหลังได้ |
| 📊 Dashboard | สรุปยอดขาย + กราฟ |
| 🔢 เลขที่ใบกำกับ | ออกแบบรูปแบบเอง + run ต่อเนื่อง |
| 💰 จำนวนเงินภาษาไทย | เช่น "สองร้อยห้าสิบเก้าบาทถ้วน" |
| 🖨️ พิมพ์ | ใบกำกับภาษีสวยงาม พร้อมใช้ |

---

## 📁 โครงสร้างไฟล์

```
bill-invoice-system/
├── index.html              # หน้าหลัก
├── css/
│   └── styles.css          # ไฟล์ CSS
├── js/
│   ├── app.js              # โค้ดหลัก
│   ├── storage.js          # จัดการ localStorage
│   ├── sheets-api.js       # เชื่อมต่อ Google Sheets
│   └── invoice.js          # สร้างใบกำกับภาษี
├── google-apps-script.js   # สำหรับ copy ไปใส่ Apps Script
└── README.md               # ไฟล์นี้
```

---

## 🎨 การปรับแต่ง

### เปลี่ยนสี Theme
แก้ไขไฟล์ `css/styles.css` ในส่วน `:root` เพื่อเปลี่ยนสี primary, success,ฯลฯ

### เปลี่ยนรูปแบบเลขที่ใบกำกับ
ไปที่ ⚙️ ตั้งค่า > รูปแบบใบกำกับ และกำหนด:
- Prefix เช่น "INV-", "TAX67-"
- เลขที่เริ่มต้น
- จำนวนหลัก

---

## ❓ คำถามที่พบบ่อย

**Q: ทำไมไม่สามารถบันทึกลง Google Sheets ได้?**
A: ตรวจสอบว่า:
1. ใส่ URL ของ Apps Script ถูกต้อง
2. Deploy เป็น "Anyone can access"
3. อนุญาตสิทธิ์การเข้าถึง

**Q: ข้อมูลหายไปหลังปิด browser?**
A: ข้อมูลถูกเก็บใน localStorage ของ browser ดังนั้น:
- อย่าล้าง browser cache
- ใช้ browser เดิมเสมอ
- แนะนำให้เชื่อมต่อ Google Sheets เพื่อสำรองข้อมูล

---

## 📞 การสนับสนุน

หากพบปัญหาในการใช้งาน กรุณาตรวจสอบ Console ของ Browser (F12 > Console) เพื่อดู error message

---

*พัฒนาด้วย ❤️ สำหรับธุรกิจไทย*
